name: inspector_app
description: Sea Secure — InspectShip Inspector App
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  image_picker: ^1.2.3
  pdf: ^3.11.1
  printing: ^5.13.1
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true